import { Component, inject } from '@angular/core'; import { RouterLink } from '@angular/router'; import { ProductCardComponent } from '../products/product-card.component'; import { ProductService } from '../../core/services/product.service';
@Component({ standalone: true, imports: [RouterLink, ProductCardComponent], template: `<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-20"><div class="grid items-center gap-10 overflow-hidden rounded-3xl bg-gradient-to-br from-violet-950 via-slate-900 to-slate-950 px-6 py-12 text-white ring-1 ring-violet-500/30 sm:px-12 lg:grid-cols-2"><div><p class="mb-3 text-sm font-semibold uppercase tracking-[.2em] text-violet-300">Tonight's escape</p><h1 class="text-4xl font-bold tracking-tight sm:text-5xl">Stories worth staying up for.</h1><p class="mt-5 max-w-lg text-lg text-slate-300">Discover memorable movies and series, selected for every kind of night.</p><a routerLink="/products" class="mt-8 inline-block rounded-xl bg-violet-500 px-5 py-3 font-semibold text-white transition hover:bg-violet-400">Browse titles →</a></div><div class="rounded-2xl bg-violet-500/10 p-8 text-center text-7xl">🎬</div></div><div class="mt-16 flex items-end justify-between"><div><p class="text-sm font-semibold text-violet-400">START WATCHING</p><h2 class="mt-1 text-3xl font-bold">Featured tonight</h2></div><a routerLink="/products" class="font-medium text-violet-400 hover:underline">See all titles</a></div><div class="mt-7 grid gap-6 md:grid-cols-2">@for (product of featuredProducts; track product.id) { <app-product-card [product]="product" /> }</div></section>` })
export class HomeComponent { private products = inject(ProductService); readonly featuredProducts = this.products.products.filter((product) => product.featured); }
