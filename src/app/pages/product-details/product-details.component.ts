import { Component, inject } from '@angular/core'; import { CurrencyPipe, DatePipe, UpperCasePipe } from '@angular/common'; import { ActivatedRoute, Router } from '@angular/router'; import { ProductService } from '../../core/services/product.service'; import { CartService } from '../../core/services/cart.service'; import { ToastService } from '../../core/services/toast.service'; import { StockStatusPipe } from '../../pipes/stock-status.pipe';
@Component({ standalone: true, imports: [CurrencyPipe, DatePipe, UpperCasePipe, StockStatusPipe], template: `<section class="mx-auto max-w-5xl px-4 py-8 sm:px-6 sm:py-12">@if (product) { <button (click)="backToProducts()" class="mb-7 font-semibold text-indigo-600">← Back to Products</button><div class="grid gap-10 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-2 md:p-10"><img [src]="product.imageUrl" [alt]="product.name" class="h-80 w-full rounded-2xl object-cover"/><div><p class="text-sm font-bold text-indigo-600">{{ product.category | uppercase }} · Product #{{ product.id }}</p><h1 class="mt-2 text-3xl font-bold sm:text-4xl">{{ product.name }}</h1><p class="mt-5 text-lg text-slate-600">{{ product.description }}</p><p class="mt-5 text-3xl font-bold">{{ product.price | currency }}</p><p class="mt-4">Status: <strong>{{ product.stock | stockStatus }}</strong> · {{ product.stock }} available</p><p class="mt-2 text-sm text-slate-500">Added on {{ product.addedOn | date:'fullDate' }}</p><div class="mt-7 flex items-center gap-4"><button (click)="decreaseQuantity()" [disabled]="quantity === 1" class="rounded-lg border px-3 py-2">−</button><span class="font-bold">Quantity: {{ quantity }}</span><button (click)="increaseQuantity()" class="rounded-lg border px-3 py-2">+</button></div><button (click)="addToCart()" [disabled]="product.stock === 0" class="mt-6 rounded-xl bg-indigo-600 px-5 py-3 font-semibold text-white disabled:bg-slate-300">Add {{ quantity }} to cart</button></div></div> } @else { <div class="rounded-2xl bg-white p-10 text-center"><h1 class="text-2xl font-bold">Product not found</h1><button (click)="backToProducts()" class="mt-4 text-indigo-600">Back to Products</button></div> }</section>` })
export class ProductDetailsComponent { private route = inject(ActivatedRoute); private products = inject(ProductService); private router = inject(Router); private cart = inject(CartService); private toast = inject(ToastService); quantity = 1; readonly product = this.products.getById(Number(this.route.snapshot.paramMap.get('id'))); increaseQuantity(): void { if (this.product && this.quantity < this.product.stock) this.quantity++; } decreaseQuantity(): void { if (this.quantity > 1) this.quantity--; } addToCart(): void { if (this.product) { for (let i = 0; i < this.quantity; i++) this.cart.add(this.product); this.toast.show(`${this.product.name} added to your watchlist`); } } backToProducts(): void { this.router.navigate(['/products']); } }
