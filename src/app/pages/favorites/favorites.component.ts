import { Component, computed, inject } from '@angular/core';
import { CurrencyPipe, DatePipe } from '@angular/common';
import { RouterLink } from '@angular/router';
import { FavoritesService } from '../../core/services/favorites.service';
import { ProductService } from '../../core/services/product.service';
import { StockStatusPipe } from '../../pipes/stock-status.pipe';
@Component({ standalone: true, imports: [RouterLink, CurrencyPipe, DatePipe, StockStatusPipe], template: `<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6"><p class="text-sm font-semibold text-indigo-600">YOUR COLLECTION</p><h1 class="mt-1 text-4xl font-bold">Favorites</h1>@if (favoritesList().length) { <div class="mt-8 grid gap-5 md:grid-cols-2">@for (product of favoritesList(); track product.id) { <article class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div class="flex gap-4"><img [src]="product.imageUrl" [alt]="product.name" class="h-24 w-24 rounded-xl object-cover"/><div><h2 class="text-xl font-bold">{{ product.name }}</h2><p class="mt-1 font-semibold">{{ product.price | currency }}</p><p class="mt-2 text-sm text-slate-600">{{ product.stock | stockStatus }} · Favorited {{ favorites.addedDates.get(product.id) | date:'mediumDate' }}</p><a [routerLink]="['/products', product.id]" class="mt-3 inline-block text-sm font-semibold text-indigo-600">View details →</a></div></div></article> }</div> } @else { <div class="mt-8 rounded-3xl bg-white p-10 text-center shadow-sm ring-1 ring-slate-200"><p class="text-4xl">♡</p><h2 class="mt-3 text-2xl font-bold">No favorites yet</h2><p class="mt-2 text-slate-600">Tap a heart on any product card to build your collection.</p><a routerLink="/products" class="mt-6 inline-block rounded-xl bg-indigo-600 px-5 py-3 font-semibold text-white">Browse products</a></div> }</section>` })
export class FavoritesComponent { readonly favorites = inject(FavoritesService); private products = inject(ProductService); readonly favoritesList = computed(() => this.products.products.filter((product) => this.favorites.ids().includes(product.id))); }
