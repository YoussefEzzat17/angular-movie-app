import { Component, inject } from '@angular/core'; import { ProductService } from '../../core/services/product.service'; import { ProductCardComponent } from './product-card.component';
@Component({ standalone: true, imports: [ProductCardComponent], template: `<section class="mx-auto max-w-6xl px-4 py-8 sm:px-6 sm:py-12"><p class="text-sm font-semibold text-violet-400">MOVIES & SERIES</p><h1 class="mt-1 text-3xl font-bold sm:text-4xl">Find your next obsession.</h1><p class="mt-3 text-slate-400">Every title card demonstrates live Angular bindings and pipes.</p><div class="mt-9 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@for (product of products.products; track product.id) { <app-product-card [product]="product" /> }</div></section>` })
export class ProductsComponent { readonly products = inject(ProductService); }
